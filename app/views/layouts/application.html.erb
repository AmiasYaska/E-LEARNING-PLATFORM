<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Coursera" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="stylesheet" href="https://cdn.plyr.io/3.7.8/plyr.css" />

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

<body class="bg-gray-100">

  <div class="flex flex-row py-8 px-8 justify-between items-center">

    <div class="flex">
      <%= link_to 'Home', videos_path %>
    </div>

    <!-- Search -->
    <div data-controller="autocomplete" class=" md:flex items-center text-center">
      <%= form_with url: videos_path, method: :get, data: {autocomplete_target: "form"}, class:"flex gap-2" do %>
        <div class="relative">
          <%= text_field_tag :search, 
            params[:search], placeholder: "Search for anything", 
            data: {
              action: "input->autocomplete#search",
              autocomplete_target: "input"
            },
            class:"border border-gray-400 rounded-full py-2 px-3 text-center" 
          %>
          <ul class="absolute w-full bg-white mt-1" data-autocomplete-target="results" data-action="click->autocomplete#selectResult"></ul>
        </div>
      <% end %>
    </div>
    <!--  Wrap Menu and Button -->
    <div data-controller="menu" class="relative w-full">

      <!--  Menu Icon for Small Screens -->
      <button data-action="click->menu#toggle" class="text-2xl md:hidden">
        ☰
      </button>

      <!-- Navbar Links -->
      <div data-menu-target="menu" class="hidden flex flex-col items-center gap-5 absolute left-1/2 transform -translate-x-1/2 top-16 w-3/4 bg-white shadow-lg rounded-lg py-5 md:static md:flex-row md:bg-transparent md:shadow-none md:rounded-none md:w-auto md:translate-x-0">

        <div class="flex flex-col items-center md:flex-row md:gap-5">
          <% if user_signed_in? %>
            <%= link_to 'My Learning', my_learning_path, class: "block md:inline text-lg" %>
            <%= link_to current_user.first_name, edit_profile_path, class: "block md:inline text-lg" %>
            <%= link_to 'Sign Out', destroy_user_session_path, data: {turbo_method: :delete, confirm: "U wanna sign Out!"}, class: "block md:inline text-lg text-red-500" %>
          <% else %>
            <%= link_to 'Log In', new_user_session_path, class: "block md:inline text-lg" %>
          <% end %>
        </div>

      </div>

</div>


  </div>

  <%= yield %>

  <%= javascript_importmap_tags %>

</body>

</html>
